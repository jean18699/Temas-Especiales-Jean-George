<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pucmm.proyectofinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal">
        <activity
            android:name=".roomviewmodel.activities.CategoryRegisterActivity"
            android:exported="true" />
        <activity
            android:name=".roomviewmodel.activities.CategoryEditActivity"
            android:exported="true" />
        <activity
            android:name=".roomviewmodel.activities.ProductEditActivity"
            android:exported="true" />
        <activity
            android:name=".roomviewmodel.activities.ProductManagerActivity"
            android:exported="true" />
        <activity
            android:name=".roomviewmodel.activities.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".roomviewmodel.activities.LoginActivity">

        </activity>
    </application>

</manifest>